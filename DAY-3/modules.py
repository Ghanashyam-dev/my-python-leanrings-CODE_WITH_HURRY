# these are the inbuilt or wirteen by someone is imported in our code for solving problems.
# their are two typesof modules in python 
# 1. builtin modules 
# are pre installed in python

# 2. external modules 
# are to be downloaded using pip install ( pip pacakge installer) or create my user .

# own module import - 1st creat a def funtion in a file , then go to the file where we need top import it , here type as import mymodule9name of the file you created the function) , to call ur func type mymodule.func_name().
import math


print(math.sqrt(16))